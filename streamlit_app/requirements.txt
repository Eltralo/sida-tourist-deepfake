# Дополнительные зависимости для streamlit_app/.
# Ставятся ПОВЕРХ основной conda-среды sida_modern, в которой уже есть
# torch, transformers, peft, opencv-python, segment-anything и т.д.
#
# Установка:
#     conda activate sida_modern
#     pip install -r streamlit_app/requirements.txt

streamlit>=1.31
plotly>=5.18
pandas>=2.0
numpy>=1.24
Pillow>=10.0
